"use client";

import { TutorI } from "@/types/tutor.types";
import { useRouter } from "next/router";

const RedirectMSAuth = ({ user }: { user: TutorI }) => {
  const router = useRouter();

  return null;
};

export default RedirectMSAuth;
